## goorm REACT ICON
created with CRA(Create React App)
### Installation
```
$ git clone https://github.com/kyoungsikchoi/goorm-icon.git
$ npm install -g
```

### Scripts
- `$ npm start`
- `$ npm test`
- `$ npm run build`
- `$ npm run eject`
